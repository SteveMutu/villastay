import ReactLoading from 'react-loading';

const Loading = () => {
    return(
        <ReactLoading className="m-auto" type="bars" color="#335c67" height={100} width={100} />
    );
}

export default Loading;